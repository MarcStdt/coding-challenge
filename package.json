{
  "name": "cosuno-challenge",
  "private": true,
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "prettier": "prettier */src/** --write",
    "test": "cypress run",
    "test:open": "cypress open",
    "clean": "rm -rf node_modules client/build server/build client/node_modules server/node_modules"
  },
  "dependencies": {
    "npm": "^7.19.1"
  },
  "devDependencies": {
    "cypress": "^7.7.0",
    "prettier": "^2.3.2"
  }
}
